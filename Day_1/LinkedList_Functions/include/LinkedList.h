#ifndef LINKEDLIST_H
#define LINKEDLIST_H
#include "Node.h"

class LinkedList
{
    public:
    Node * head ;
    Node * tail ;

        LinkedList()
         {
           head = NULL;
           tail = NULL;
         }

        ~LinkedList() {}

        void add(int data)
        {
           if (head == NULL)
           {

           }
        }

//    protected:

//    private:
};

#endif // LINKEDLIST_H
